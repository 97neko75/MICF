# 贡献指南（写给想帮忙的“杂鱼”们）

既然你点进来了，说明你对这个破烂内核有点兴趣？ 
那么本大小姐就下规矩了！

## 1. 先保证你能编译

装了 **i686-elf-gcc** 和 **QEMU** 
运行 `build.bat` 能否生成 `micf.elf`

如果不行，先解决自己的环境问题（网上教程一堆），别来问这种问题来烦我。

## 2. 你想改什么？

当前急需帮忙的地方：

- [ ] **支持左右方向键移动光标**（梦一个行内编辑）
- [ ] **给 README 加上真正的截图**（这破内核长啥样别人都不知道）
- [ ] **其他的以后再说**

你挑一个能做的不吹牛的去做。

## 3. 提 PR 的流程

1. **Fork** 本仓库到你自己的 GitHub。
2. **创建一个分支**，名字用 `fix/xxx` 或 `feature/xxx`，别在主分支上乱改。
3. **改完后自己先编译测试**，确保至少 `help`、`fortune` 等命令还能用。
4. **提交 PR** 到本仓库的 `main` 分支。  
   - 标题简明扼要，例如 `修复退格键重复删除问题`。  
   - 内容里说清楚你改了哪里、怎么改的、测试结果如何（附上截图更佳）。
5. 等着我（或未来其他维护者）审核。如果过了就合，没过我会告诉你原因（可能骂你）。

## 4. 一个忠告

**别试图把 MICF 加入NTFS，企业项目没有一个好惹的**  它就是个玩具
你要是真想贡献，就帮我把现有的这点破代码整理干净，让后来的人看得懂就行。
实在是要修改，MIT许可证我也管不到

## 5. 关于版权

你提交的代码默认采用 **MIT 许可证**，和本仓库一样。别抄别人的代码不注明来源，否则我会把你挂到 `Hall of Shame`。

---

好了，该说的都说了。  
现在，滚去写代码吧，垃圾。
